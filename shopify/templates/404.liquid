<section class="section center">
  <div class="container-x">
    <h1>Page not found</h1>
    <p style="color:var(--muted-foreground)">The page you are looking for has moved or sold out.</p>
    <a class="btn btn-gold" href="{{ routes.root_url }}">Back to home</a>
  </div>
</section>
