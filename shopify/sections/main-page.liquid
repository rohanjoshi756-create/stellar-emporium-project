{%- comment -%} Static page template body. {%- endcomment -%}
<section class="section">
  <div class="container-x" style="max-width:900px">
    <h1>{{ page.title }}</h1>
    <div style="margin-top:1.25rem;font-size:.95rem;color:var(--muted-foreground)">{{ page.content }}</div>
  </div>
</section>
{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
